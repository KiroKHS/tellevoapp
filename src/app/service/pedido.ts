export class Pedido {
  // eslint-disable-next-line @typescript-eslint/naming-convention
  id_pedido: number;
	// eslint-disable-next-line @typescript-eslint/naming-convention
	id_chofer: number;
	pasajero: string;
	direccion: string;
	hora: string;
	estado: number;
}
