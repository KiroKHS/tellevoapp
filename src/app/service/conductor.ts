export class Conductor {
  // eslint-disable-next-line @typescript-eslint/naming-convention
  id_conductor: number;
  conductor: string;
  entrada: string;
  salida: string;
  silla: string;
  matricula: string;
  costo: number;
}
